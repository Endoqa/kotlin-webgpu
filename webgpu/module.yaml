product:
  type: lib
  platforms: [ jvm, android ]

dependencies:
  - ../webgpu-c


apply:
  - ../common.module-template.yaml
settings:
  kotlin:
    optIns: [ kotlin.ExperimentalUnsignedTypes ]
    freeCompilerArgs:
      - -Xexpect-actual-classes